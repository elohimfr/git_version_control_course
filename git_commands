

1. git init  | start version control (vc) in a directory
2. git add   | add a file in the directory to vc 
3. git commit -a   | save a snapshot version of the dir
4. git status   | list the changes since the last version
5. git log   | get a log of all versions
6. git checkout [version number]   | change version
7. git checkout master   | return to the latest version
8. git checkout -b branch_name   | create a new branch
9. git push   | push the .git file to the cloud
10. git pull   | pull the cloud .git to my computer
11. git clone   | 


git education
continuous integration (travis)
continuous deployment
rse research software engineerings association
